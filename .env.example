# Supabase — Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase — Project Settings > API keys (secret, server-only, nunca NEXT_PUBLIC_)
# Usada para crear cuentas de socios desde /cuenta sin depender del envío de email.
SUPABASE_SERVICE_ROLE_KEY=
